services:
  crawler-bs:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: crawler-test-bs
    volumes:
      # Persist Crawlee storages (dataset, key-value store, request queue) on host
      - ./storage:/usr/src/app/storage
    command: ["python", "-m", "crawler_test_bs"]
